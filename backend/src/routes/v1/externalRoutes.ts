import { Router } from 'express';

const router = Router();

/**
 * @summary
 * External (public) routes configuration
 * Routes accessible without authentication
 *
 * @route /api/v1/external
 */

export default router;
